# External Language Models

Place an existing KenLM language model here, for example:

```text
english_3gram.arpa
english_3gram.bin
```

The decoder accepts `.arpa` and `.bin` files through `--lm_path`. Language
model files are not generated or downloaded by this repository setup.
